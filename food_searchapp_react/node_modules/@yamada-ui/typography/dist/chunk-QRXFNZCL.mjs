"use client"

// src/text.tsx
import {
  ui,
  forwardRef,
  omitThemeProps,
  useComponentStyle
} from "@yamada-ui/core";
import { cx, filterUndefined } from "@yamada-ui/utils";
import { jsx } from "react/jsx-runtime";
var Text = forwardRef((props, ref) => {
  const [css, mergedProps] = useComponentStyle("Text", props);
  const {
    className,
    align: textAlign,
    decoration: textDecoration,
    casing: textTransform,
    ...rest
  } = omitThemeProps(mergedProps);
  const textProps = filterUndefined({
    textAlign,
    textDecoration,
    textTransform
  });
  return /* @__PURE__ */ jsx(
    ui.p,
    {
      ref,
      className: cx("ui-text", className),
      __css: css,
      ...textProps,
      ...rest
    }
  );
});

export {
  Text
};
//# sourceMappingURL=chunk-QRXFNZCL.mjs.map