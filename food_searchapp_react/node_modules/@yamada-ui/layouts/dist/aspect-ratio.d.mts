import * as _yamada_ui_core from '@yamada-ui/core';
import { HTMLUIProps, Token } from '@yamada-ui/core';

type AspectRatioOptions = {
    /**
     * The aspect ratio of the Box.
     *
     * @default '4 / 3'
     */
    ratio?: Token<number>;
};
type AspectRatioProps = HTMLUIProps<"div"> & AspectRatioOptions;
/**
 * `AspectRatio` is a component for embedding things like videos and maps while maintaining the aspect ratio.
 *
 * @see Docs https://yamada-ui.com/components/layouts/aspect-ratio
 */
declare const AspectRatio: _yamada_ui_core.Component<"div", AspectRatioProps>;

export { AspectRatio, type AspectRatioProps };
