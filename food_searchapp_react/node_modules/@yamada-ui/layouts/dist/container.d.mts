import * as _yamada_ui_core from '@yamada-ui/core';
import { HTMLUIProps, ThemeProps } from '@yamada-ui/core';

type ContainerOptions = {
    /**
     * If `true`, container will center its children regardless of their width.
     *
     * @default false
     */
    centerContent?: boolean;
};
type ContainerProps = HTMLUIProps<"section"> & ThemeProps<"Container"> & ContainerOptions;
/**
 * `Container` is a component used as a general division element. By default, it renders the `section` element.
 *
 * @see Docs https://yamada-ui.com/components/layouts/container
 */
declare const Container: _yamada_ui_core.Component<"section", ContainerProps>;

export { Container, type ContainerProps };
