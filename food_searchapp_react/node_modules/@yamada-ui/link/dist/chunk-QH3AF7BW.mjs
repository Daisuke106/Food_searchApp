"use client"

// src/link.tsx
import {
  ui,
  forwardRef,
  omitThemeProps,
  useComponentStyle
} from "@yamada-ui/core";
import { cx } from "@yamada-ui/utils";
import { jsx } from "react/jsx-runtime";
var Link = forwardRef((props, ref) => {
  const [css, mergedProps] = useComponentStyle("Link", props);
  const { className, isExternal, ...rest } = omitThemeProps(mergedProps);
  return /* @__PURE__ */ jsx(
    ui.a,
    {
      ref,
      target: isExternal ? "_blank" : void 0,
      rel: isExternal ? "noopener" : void 0,
      className: cx("ui-link", className),
      __css: css,
      ...rest
    }
  );
});

export {
  Link
};
//# sourceMappingURL=chunk-QH3AF7BW.mjs.map