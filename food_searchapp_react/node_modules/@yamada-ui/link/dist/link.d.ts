import * as _yamada_ui_core from '@yamada-ui/core';
import { HTMLUIProps, ThemeProps } from '@yamada-ui/core';

type LinkOptions = {
    /**
     * If `true`, the link will open in new tab.
     *
     * @default false
     */
    isExternal?: boolean;
};
type LinkProps = HTMLUIProps<"a"> & ThemeProps<"Link"> & LinkOptions;
/**
 * `Link` is a component for creating hyperlinks to different web pages, locations within the same page, or other URLs.
 *
 * @see Docs https://yamada-ui.com/components/navigation/link
 */
declare const Link: _yamada_ui_core.Component<"a", LinkProps>;

export { Link, type LinkProps };
