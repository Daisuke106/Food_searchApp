"use client"

// src/badge.tsx
import {
  ui,
  forwardRef,
  useComponentStyle,
  omitThemeProps
} from "@yamada-ui/core";
import { cx } from "@yamada-ui/utils";
import { jsx } from "react/jsx-runtime";
var Badge = forwardRef((props, ref) => {
  const [styles, mergedProps] = useComponentStyle("Badge", props);
  const { className, ...rest } = omitThemeProps(mergedProps);
  const css = {
    display: "inline-block",
    whiteSpace: "nowrap",
    verticalAlign: "middle",
    ...styles
  };
  return /* @__PURE__ */ jsx(
    ui.span,
    {
      ref,
      className: cx("ui-badge", className),
      __css: css,
      ...rest
    }
  );
});

export {
  Badge
};
//# sourceMappingURL=chunk-URGLF2HC.mjs.map