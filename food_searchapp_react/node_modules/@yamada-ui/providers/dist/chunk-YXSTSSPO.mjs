"use client"
import {
  EnvironmentProvider
} from "./chunk-4UFTGRYY.mjs";

// src/ui-provider.tsx
import {
  ThemeProvider,
  ColorModeProvider,
  ResetStyle,
  GlobalStyle
} from "@yamada-ui/core";
import { LoadingProvider } from "@yamada-ui/loading";
import { MotionConfig } from "@yamada-ui/motion";
import { NoticeProvider } from "@yamada-ui/notice";
import { defaultTheme, defaultConfig } from "@yamada-ui/theme";
import { jsx, jsxs } from "react/jsx-runtime";
var UIProvider = ({
  theme = defaultTheme,
  config = defaultConfig,
  disableResetStyle,
  disableGlobalStyle,
  colorModeManager,
  colorModeStorageKey,
  themeSchemeManager,
  themeSchemeStorageKey,
  environment,
  disableEnvironment,
  children
}) => {
  var _a;
  return /* @__PURE__ */ jsx(
    ThemeProvider,
    {
      theme,
      config,
      themeSchemeManager,
      storageKey: themeSchemeStorageKey,
      children: /* @__PURE__ */ jsx(
        ColorModeProvider,
        {
          colorModeManager,
          storageKey: colorModeStorageKey,
          config,
          children: /* @__PURE__ */ jsx(
            EnvironmentProvider,
            {
              environment,
              disabled: disableEnvironment,
              children: /* @__PURE__ */ jsx(MotionConfig, { ...(_a = config.motion) == null ? void 0 : _a.config, children: /* @__PURE__ */ jsxs(LoadingProvider, { ...config.loading, children: [
                !disableResetStyle ? /* @__PURE__ */ jsx(ResetStyle, {}) : null,
                !disableGlobalStyle ? /* @__PURE__ */ jsx(GlobalStyle, {}) : null,
                children,
                /* @__PURE__ */ jsx(NoticeProvider, { ...config.notice })
              ] }) })
            }
          )
        }
      )
    }
  );
};

export {
  UIProvider
};
//# sourceMappingURL=chunk-YXSTSSPO.mjs.map