import { ReactNode, FC } from 'react';

type Environment = {
    getWindow: () => Window | undefined;
    getDocument: () => Document | undefined;
};
type EnvironmentProviderProps = {
    children: ReactNode;
    disabled?: boolean;
    environment?: Environment;
};
declare const EnvironmentProvider: FC<EnvironmentProviderProps>;
declare const useEnvironment: ({ isDefer }?: {
    isDefer?: boolean;
}) => Environment;

export { type Environment, EnvironmentProvider, type EnvironmentProviderProps, useEnvironment };
