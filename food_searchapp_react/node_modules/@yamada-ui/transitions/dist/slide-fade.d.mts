import * as _yamada_ui_core from '@yamada-ui/core';
import { Token } from '@yamada-ui/core';
import { WithTransitionProps, MotionProps, MotionTransitionVariants } from '@yamada-ui/motion';

type SlideFadeOptions = {
    /**
     * The offset on the horizontal or `x` axis.
     *
     * @default 0
     */
    offsetX?: Token<string | number>;
    /**
     * The offset on the vertical or `y` axis.
     *
     * @default 8
     */
    offsetY?: Token<string | number>;
    /**
     * If `true`, the element will be transitioned back to the offset when it leaves. Otherwise, it'll only fade out.
     *
     * @default true
     */
    reverse?: boolean;
};
type SlideFadeProps = WithTransitionProps<MotionProps> & SlideFadeOptions;
declare const slideFadeProps: {
    initial: string;
    animate: string;
    exit: string;
    variants: MotionTransitionVariants;
};
/**
 * `SlideFade` is a component that gradually shows or hides an element while moving it from a specified position.
 *
 * @see Docs https://yamada-ui.com/components/transitions/slide-fade
 */
declare const SlideFade: _yamada_ui_core.Component<"div", SlideFadeProps>;

export { SlideFade, type SlideFadeProps, slideFadeProps };
