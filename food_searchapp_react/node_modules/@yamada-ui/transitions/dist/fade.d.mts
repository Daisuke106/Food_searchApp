import * as _yamada_ui_core from '@yamada-ui/core';
import { WithTransitionProps, MotionProps, MotionTransitionVariants } from '@yamada-ui/motion';

type FadeProps = WithTransitionProps<MotionProps>;
declare const fadeProps: {
    initial: string;
    animate: string;
    exit: string;
    variants: MotionTransitionVariants;
};
/**
 * `Fade` is a component that gradually shows or hides an element.
 *
 * @see Docs https://yamada-ui.com/components/transitions/fade
 */
declare const Fade: _yamada_ui_core.Component<"div", FadeProps>;

export { Fade, type FadeProps, fadeProps };
