import * as _yamada_ui_core from '@yamada-ui/core';
import { WithTransitionProps, MotionProps, MotionTransitionVariants } from '@yamada-ui/motion';

type ScaleFadeOptions = {
    /**
     * The initial scale of the element.
     *
     * @default 0.95
     */
    scale?: number;
    /**
     * If `true`, the element will transition back to exit state.
     *
     * @default true
     */
    reverse?: boolean;
};
type ScaleFadeProps = WithTransitionProps<Omit<MotionProps<"div">, "scale">> & ScaleFadeOptions;
declare const scaleFadeProps: {
    initial: string;
    animate: string;
    exit: string;
    variants: MotionTransitionVariants;
};
/**
 * `ScaleFade` is a component that gradually scales up to reveal or scales down to hide an element.
 *
 * @see Docs https://yamada-ui.com/components/transitions/scale-fade
 */
declare const ScaleFade: _yamada_ui_core.Component<"div", ScaleFadeProps>;

export { ScaleFade, type ScaleFadeProps, scaleFadeProps };
