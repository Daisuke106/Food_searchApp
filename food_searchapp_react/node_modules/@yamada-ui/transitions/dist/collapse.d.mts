import * as _yamada_ui_core from '@yamada-ui/core';
import { WithTransitionProps, MotionProps, MotionTransitionVariants } from '@yamada-ui/motion';

type CollapseOptions = {
    /**
     * If `true`, the opacity of the content will be animated.
     *
     * @default true
     */
    animationOpacity?: boolean;
    /**
     * The height you want the content in its collapsed state.
     *
     * @default 0
     */
    startingHeight?: number | string;
    /**
     * The height you want the content in its expanded state.
     *
     * @default "auto"
     */
    endingHeight?: number | string;
};
type CollapseProps = WithTransitionProps<MotionProps> & CollapseOptions;
declare const collapseProps: {
    initial: string;
    animate: string;
    exit: string;
    variants: MotionTransitionVariants;
};
/**
 * `Collapse` is a component that allows you to expand or collapse an element for display.
 *
 * @see Docs https://yamada-ui.com/components/transitions/collapse
 */
declare const Collapse: _yamada_ui_core.Component<"div", CollapseProps>;

export { Collapse, type CollapseProps, collapseProps };
