"use client"

// src/table-container.tsx
import { ui, forwardRef } from "@yamada-ui/core";
import { cx } from "@yamada-ui/utils";
import { jsx } from "react/jsx-runtime";
var TableContainer = forwardRef(
  ({ className, overflow, overflowX, ...rest }, ref) => {
    var _a;
    const css = {
      display: "block",
      whiteSpace: "nowrap",
      WebkitOverflowScrolling: "touch",
      overflowX: (_a = overflow != null ? overflow : overflowX) != null ? _a : "auto",
      overflowY: "hidden",
      maxW: "100%"
    };
    return /* @__PURE__ */ jsx(
      ui.div,
      {
        ref,
        className: cx("ui-table__container", className),
        __css: css,
        ...rest
      }
    );
  }
);

export {
  TableContainer
};
//# sourceMappingURL=chunk-DIW3KZOZ.mjs.map