type OS = "undetermined" | "macos" | "ios" | "windows" | "android" | "linux";
/**
 * `useOS` is a custom hook that returns the user's OS.
 *
 * @see Docs https://yamada-ui.com/hooks/use-os
 */
declare const useOS: () => OS;

export { type OS, useOS };
