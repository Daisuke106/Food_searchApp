import { UseSnacksOptions, UseSnacksReturn } from './use-snacks.js';
import * as _yamada_ui_core from '@yamada-ui/core';
import { ThemeConfig } from '@yamada-ui/core';
import { MotionProps, MotionVariants } from '@yamada-ui/motion';
import '@yamada-ui/alert';
import 'react';

type SnacksOptions = {
    snacks: UseSnacksReturn["snacks"];
    /**
     * The variants of the snacks container.
     * Check the docs to see the variants of possible modifiers you can pass.
     *
     * @see Docs https://www.framer.com/motion/animation/#variants
     */
    containerVariants?: MotionVariants;
    /**
     * The variants of the snacks list.
     * Check the docs to see the variants of possible modifiers you can pass.
     *
     * @see Docs https://www.framer.com/motion/animation/#variants
     */
    listVariants?: MotionVariants;
    /**
     * Props for the snacks list element.
     */
    listProps?: MotionProps;
};
type SnacksProps = Omit<MotionProps<"div">, "direction"> & SnacksOptions & Pick<UseSnacksOptions, "direction" | "startIndex"> & Pick<Required<ThemeConfig>["snacks"], "variants" | "gutter" | "negateMargin">;
declare const Snacks: _yamada_ui_core.Component<"div", SnacksProps>;

export { Snacks, type SnacksProps };
