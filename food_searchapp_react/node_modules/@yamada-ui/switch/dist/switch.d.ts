import * as _yamada_ui_core from '@yamada-ui/core';
import { HTMLUIProps, ThemeProps } from '@yamada-ui/core';
import { UseCheckboxProps } from '@yamada-ui/checkbox';
import { MotionTransition } from '@yamada-ui/motion';
import { Merge } from '@yamada-ui/utils';
import { ReactElement, InputHTMLAttributes } from 'react';

type SwitchIconProps = {
    isChecked?: boolean;
    isFocused?: boolean;
    isHovered?: boolean;
    isActive?: boolean;
};
type SwitchOptions = {
    /**
     * The switch icon to use.
     */
    icon?: ReactElement<SwitchIconProps>;
    /**
     * Props for switch input element.
     */
    inputProps?: InputHTMLAttributes<HTMLInputElement>;
    /**
     * Props for switch label element.
     */
    labelProps?: HTMLUIProps<"span">;
    /**
     * Change switch label from right to left.
     *
     * @default false
     */
    isReverse?: boolean;
    /**
     * Motion transition settings.
     */
    transition?: MotionTransition;
};
type SwitchProps = Merge<HTMLUIProps<"label">, Omit<UseCheckboxProps, "isIndeterminate">> & ThemeProps<"Switch"> & SwitchOptions;
/**
 * `Switch` is a component used to toggle between on and off states.
 *
 * @see Docs https://yamada-ui.com/components/forms/switch
 */
declare const Switch: _yamada_ui_core.Component<"input", SwitchProps>;

export { Switch, type SwitchIconProps, type SwitchProps };
