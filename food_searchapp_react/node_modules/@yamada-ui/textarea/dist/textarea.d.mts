import * as _yamada_ui_core from '@yamada-ui/core';
import { HTMLUIProps, ThemeProps, ColorModeToken, CSS } from '@yamada-ui/core';
import { FormControlOptions } from '@yamada-ui/form-control';
import { ForwardedRef } from 'react';

type TextareaOptions = {
    /**
     * The border color when the input is focused.
     */
    focusBorderColor?: ColorModeToken<CSS.Property.BorderColor, "colors">;
    /**
     * The border color when the input is invalid.
     */
    errorBorderColor?: ColorModeToken<CSS.Property.BorderColor, "colors">;
    /**
     * If `true`, the Textarea height auto-adjusts to text height.
     */
    autosize?: boolean;
    /**
     * Autosize up to maxRows rows.
     *
     * @default Infinity
     */
    maxRows?: number;
    /**
     * Autosize up to minRows rows.
     *
     * @default 1
     */
    minRows?: number;
    /**
     * Ref to a resize function.
     */
    resizeRef?: ForwardedRef<() => void>;
};
type TextareaProps = Omit<HTMLUIProps<"textarea">, "disabled" | "required" | "readOnly"> & ThemeProps<"Textarea"> & TextareaOptions & FormControlOptions;
/**
 * `Textarea` is a component used to obtain multi-line text input.
 *
 * @see Docs https://yamada-ui.com/components/forms/textarea
 */
declare const Textarea: _yamada_ui_core.Component<"textarea", TextareaProps>;

export { Textarea, type TextareaProps };
