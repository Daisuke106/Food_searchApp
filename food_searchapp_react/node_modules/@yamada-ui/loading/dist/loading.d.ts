import * as _yamada_ui_core from '@yamada-ui/core';
import { ThemeProps, CSSUIProps } from '@yamada-ui/core';
import { IconProps } from '@yamada-ui/icon';

type LoadingOptions = {
    /**
     * The variant of the Loading.
     *
     * @default 'oval'
     */
    variant?: "oval" | "grid" | "audio" | "dots" | "puff" | "rings" | "circles";
    /**
     * The CSS `color` property.
     *
     * @default 'primary'
     */
    secondaryColor?: CSSUIProps["color"];
    /**
     * The CSS `dur` property.
     */
    duration?: IconProps["dur"];
};
type LoadingProps = IconProps & Pick<ThemeProps<"Loading">, "colorScheme"> & LoadingOptions;
/**
 * `Loading` is a component displayed during waiting times, such as when data is being loaded.
 *
 * @see Docs https://yamada-ui.com/components/feedback/loading
 */
declare const Loading: _yamada_ui_core.Component<"svg", LoadingProps>;

export { Loading, type LoadingProps };
